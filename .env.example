# Cloudflare Web Analytics beacon token.
# Get it from: Cloudflare dashboard → Web Analytics → your site → the
# data-cf-beacon token in the snippet it shows you.
#
# Set this in your Cloudflare Pages project (Settings → Environment variables),
# NOT here in the repo. Leave it unset locally so analytics stays off in dev.
PUBLIC_CF_BEACON_TOKEN=
